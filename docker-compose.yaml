version: "3.9"

services:
  chatbot-backend:
    build: .
    container_name: chatbot-backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./app:/app/app
      - ./chroma_data:/app/chroma_data
    restart: unless-stopped